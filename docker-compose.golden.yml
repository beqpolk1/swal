services:
    nginx:
        volumes:
            - ./services/web/user_golden:/www/user
            
    db:
        volumes:
            - mongo_golden:/data/db
            
            
volumes:
    mongo_golden:
            
